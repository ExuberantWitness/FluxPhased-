# 详细修改方案：冲刺《Engineering Applications of Artificial Intelligence》(EAAI, Q1)

**定位:** 这份方案**取代** `EXPERIMENT_DESIGN_Q1.md` 的目标期刊与贡献角度,并补齐它对 EAAI 缺的部分。
EAAI 不是方法类 ML 会议(TPAMI/ICML/NeurIPS),它是**工程应用**期刊(IF≈8,Q1)。审稿人要的是:
真实工程问题 + 方法严谨 + **强 baseline 对比** + **真实性/可部署性论证** + 鲁棒性/泛化 + 统计严谨。
当前工作离这个标准的差距,按重要性排序,就是下面 6 个工作包(WP)。

> **诚实前置**:即便全部做完,纯仿真 + 军事色彩场景投 EAAI 仍有真实风险(见 §7 风险与备选)。
> 本方案给出**命中 EAAI 的最可信路径**,但 claim 必须严格对齐 evidence——这是上一版报告最大的隐患。

---

## WP0. 重新定位与贡献重述(不花 GPU,但决定全文成败)

### 0.1 目标期刊错位 → 改定位
- 旧:"首个 alpha-star 风格联赛"(方法新颖性导向 → 适合 ML 会议,EAAI 会嫌"算法增量")。
- 新:**"认知/协同多功能相控阵雷达 + 多智能体 RL 的传感-火控资源管理框架,在角分辨受限物理下实现亚米级精确交战"**——工程问题导向。

### 0.2 把真正的工程贡献摆到台前(目前被埋没)
论文的**一类贡献(C1,工程)**应是这条**物理可泛化**的洞察,而非联赛:
> **部署几何(≥5km 基线)+ 多基地 cm-级距离三角融合 + 残差控制**,把"测距准、测角糊(几十米)"的雷达物理,
> 转成**亚米级**定位与交战——零额外算量、物理正确、可迁移到任意分布式雷达网。
**二类贡献(C2,方法)**才是 MARL 联赛(作为训练这套控制器的手段),且**必须证明它打赢外部 baseline 才算贡献**。

### 0.3 场景去敏 + 双用途重述
- 把 "laser precise-kill" 重述为 **"定向能/精确末端交战作动器"** 下的**协同传感-资源管理**;
- 强调双用途:**反无人机、空间态势感知、协同传感网资源调度**——civilian engineering 落点,降低审稿不适。

---

## WP1. 把核心系统跑到"可信收敛"(发表的入场门,~1 周)

**现状**:联赛只跑过 2–3 轮调试,kr 只到 6–70m(非 0.2m),22% 评估仍 0.5。**这不是结果,是 debug 痕迹。**

**任务**:把 `laser_25x25_pro6000_league.yaml` 跑满 **30 iters × ≥1 seed** 到收敛,先证明联赛路径自己能work。
应用 `EXPERIMENT_DESIGN_Q1.md §7` 的两个配置改动(n_eval=50、kr_init=100/decay=0.7/threshold=0.7)。

**通过判据(全部满足才算门过):**
| 指标 | 阈值 |
|---|---|
| Final kill_radius(联赛路径) | ≤ 0.5m(逼近 train_laser 的 0.2m) |
| 残余 0.50 占比 | < 10%(当前 22%) |
| Final NashConv | < 0.05 |
| 课程单调退火 | kr 全程不反弹回升 |
| 复现 | 至少 1 seed 完整、log 留档 |

**若门过不了**(kr 卡 >5m / 0.5 占比降不下):说明联赛路径仍有问题,**先停下定位,不要进 WP2**。

---

## WP2. 外部 baseline(Q1 的非妥协项,最缺的一块,~3 周)

旧设计的 cell A–G **全是自家内部变体**(联赛 vs PSRO-lite vs self-play)。Q1 要求对比**文献既有方法**。
新增 4 条**外部 baseline**,全部在**同一环境、同一传感/奖励、同一预算**下对比:

| Baseline | 类别 | 实现路径 | 为什么必须有 |
|---|---|---|---|
| **IPPO** | 独立多智能体 | 关 CTDE,每 agent 独立 PPO(代码已具备) | 最弱 MARL 下限,证明协同有价值 |
| **MAPPO** | CTDE 集中 critic | 已有 TeamCritic,标准化为 MAPPO 配置 | 当前 MARL 主流强 baseline |
| **QMIX** | 值分解 | 接 PyMARL/标准实现到本环境(PettingZoo 已封装,~3–5 天) | 审稿人必问的值分解对照 |
| **经典优化控制器(非 RL)** | 工程基线 | 贪心/MPC 波束-火控调度器,用同一多基地融合做估计,规则触发开火 | **EAAI 关键**:证明"AI 比工程经典法强",否则审稿问"为何不用传统方法" |

**通过判据**:cell A(完整联赛)在**样本效率**(达 kr=1m 的 GPU-hours)与**对未见对手鲁棒性**(held-out exploitability)上,
**统计显著**优于上述 4 条(§5 统计协议)。**经典控制器对比是 EAAI 区别于 ML 会议的命门,必须做。**

---

## WP3. 工程真实性锚点(EAAI 的命门,最难也最决定成败,~3–4 周)

**问题**:全仿真、只对了 MATLAB(另一个仿真)。EAAI 审稿人会直接问"这能用在真系统吗"。
**不需要造真雷达**,但必须有下面的**真实性三件套**:

### 3.1 CRLB 物理锚点(强、低成本、必做)
- 对给定部署几何 + SNR,算多基地定位的 **Cramér–Rao 下界**(Godrich/Haimovich 公式);
- 证明学到的控制器**定位 RMSE 在 CRLB 的 X% 以内**,且亚米精度**不违反物理下界**;
- 产出图:`定位RMSE vs 几何/SNR`,叠 CRLB 曲线。**这把"亚米"从仿真断言升级为物理可达性证明。**

### 3.2 现实损伤注入 + 鲁棒性(必做)
当前环境太"干净"。加入并扫描:
| 损伤 | 模型 | 论文呈现 |
|---|---|---|
| 地/海杂波 | Weibull / K 分布 | 杂波强度 vs 性能曲线 |
| 多径 | 双径/瑞利 | 有/无多径对比 |
| 量测偏置+延迟 | 偏置项 + control-to-act 延迟 | 延迟扫描鲁棒性 |
| 有限波束捷变/功率/占空比 | 硬约束 | 受约束 vs 理想 |
| 通信速率限制(ISAC) | 估计经 BPSK 差分编码回传 | 速率 vs 精度权衡(RESULTS_SUMMARY 已有此线) |
- 产出:**鲁棒性表**(每种损伤下的性能保持率),证明系统不是只在理想仿真里work。

### 3.3 真数据校验传感前端(强加分,尽量做)
- 用**公开雷达数据集**(如 RadarScenes,或公开实测 RCS / I-Q)校验探测+定位前端的统计特性;
- 哪怕全闭环是仿真,**前端经真数据校验**会极大提升 EAAI 可信度。若实在拿不到,退守 3.1+3.2。

---

## WP4. 泛化研究(Q1 期望,~2 周,与 WP3 并行)

单一 25×25/2-team/固定几何**不够**。做**跨场景泛化**(train 一套,test 未见):
- 目标动力学:静止 → 匀速 → 机动(已有 Kalman tracked 模式支撑);
- 部署几何:随机基线/角度;雷达数 2→3→4;队伍数 2→N;
- EW 条件:无干扰 → 干扰 → 暴露博弈(ew_exposure);
- **产出**:泛化矩阵(train条件 × test条件 热图),报告 OOD 退化%。

---

## WP5. 内部消融(沿用旧设计 A–G,但补 2 项,~并入 WP2/4 预算)

旧 cell D/E/F/G(去 TC-DAMS/课程/CTDE/exploiter)**保留**,另补两项对应 C1 工程贡献:
- **去部署基线**(min_radar_baseline=0):预期性能崩(回到 0.5 病根)——证明 C1 的因果;
- **传感模式阶梯**:single → fused → tracked(Kalman)——量化每级对精度的贡献。
每项报告 `退化% = (metric_A − metric_X)/metric_A`,附效应量。

---

## WP6. 统计与 claim 纪律(贯穿,Q1 硬要求)

### 6.1 统计协议(写进方法学)
- **≥5 seeds**(旧设计 3 偏少;主对比至少 5);
- 报 **mean ± 95% CI**(bootstrap,1e4 重采样);
- 成对显著性:**Welch's t**(或非参 Mann–Whitney),报 **效应量 Cohen's d**;
- 多重比较:**Holm–Bonferroni** 校正;
- 学习曲线:均值 + CI 阴影带;样本效率用**达阈所需 GPU-hours**与 **AUC**两个口径。

### 6.2 claim = evidence(纪律)
- 删除一切"完全解决/验证了"等超出数据的措辞;
- 每条 claim 必须能指到一张图/表/显著性检验;
- **诚实负结果**:若联赛在某指标不优于 MAPPO,如实报告并讨论——Q1 审稿欣赏诚实,惩罚过度宣称。

---

## §4 修订实验矩阵(总览)

```
主对比(WP1+WP2):  {IPPO, QMIX, MAPPO, 经典控制器, PSRO-lite, FluxLeague-full}  × 5 seeds
内部消融(WP5):     {去TC-DAMS, 去课程, 去CTDE, 去exploiter, 去部署基线, 传感阶梯×3} × 3 seeds
真实性(WP3):       CRLB曲线 + 5类损伤扫描 + (真数据前端校验)
泛化(WP4):         {目标动力学×3, 几何随机, 雷达数×3, EW×3} train/test 热图
```

## §5 核心指标(论文表格)
| 指标 | 口径 | 目标(主方法) |
|---|---|---|
| Final kill_radius | m | ≤ 0.5(联赛路径) |
| 定位 RMSE / CRLB | 比值 | ≤ 1.3×(逼近物理下界) |
| 样本效率 | 达 kr=1m 的 GPU-hours | 显著 < 各 baseline |
| Held-out exploitability | vs 未见对手胜率 | > 0.7 |
| 鲁棒性保持率 | 各损伤下性能/理想 | 报告全谱 |
| NashConv / effK | 收敛性 | <0.05 / >3 |

## §6 GPU 预算与时间线(现实版)
| 阶段 | 内容 | GPU-h | 说明 |
|---|---|---|---|
| WP1 | 收敛门 1 seed | ~60 | 不过则止损 |
| WP2 | 6 法 × 5 seed × ~40h | ~1200 | 含外部 baseline |
| WP3 | 损伤扫描 + CRLB | ~300 | CRLB 几乎零 GPU |
| WP4 | 泛化矩阵 | ~400 | 多为 eval |
| WP5 | 6 消融 × 3 seed | ~720 | |
| 缓冲 | 失败重跑 30% | ~800 | |
| **合计** | | **~3500 GPU-h** | 旧估 1655 偏乐观(漏了外部 baseline/真实性/泛化) |
- 单 PRO 6000 串行 ≈ 5 个月;**+4090/H100/集群并行 → 2–2.5 个月**;
- **强烈建议**:WP1 门过后,先并行 WP2 主对比(决定贡献是否成立)再投入 WP3–5。

## 论文结构(EAAI 体例)
1. Intro:工程问题(角分辨受限下的协同精确交战)+ C1/C2 贡献;
2. Related work:认知雷达 / MARL-雷达资源管理 / 多基地定位(CRLB)/ 残差 RL / PSRO-league——明确空白;
3. System:仿真器(MATLAB+CRLB 双校验)+ 物理建模 + 损伤;
4. Method:部署几何+多基地融合+残差控制(C1)+ 联赛训练框架(C2);
5. Experiments:WP2 主对比 + WP5 消融 + WP3 真实性/鲁棒 + WP4 泛化;
6. Discussion:可部署性、局限(纯仿真)、双用途;
7. Conclusion。

---

## §7 风险与备选venue(诚实)
| 风险 | 缓解 |
|---|---|
| 联赛不优于 MAPPO(WP2) | 贡献重心移到 C1 工程洞察 + 框架,联赛降为"训练手段之一" |
| 纯仿真被 EAAI 拒 | WP3 真数据前端校验拉满;或转投 **IEEE TAES / TNNLS / Neurocomputing / IEEE T-Radar**(对纯仿真更友好) |
| GPU 不够 | 主对比降到 3 seeds + 20 iters,泛化用 eval-only |
| 军事色彩审稿不适 | WP0.3 双用途重述,实验加 civilian 反无人机/SSA 场景 |

**备选venue阶梯**(命中率从高到低对纯仿真):IEEE TAES → Neurocomputing → EAAI → (实测/硬件后)更高。
EAAI 现实命中前提 = **WP3 真实性三件套至少做满 3.1+3.2,且 WP2 外部 baseline 显著胜出**。

---

## §8 立即决策点
| 选项 | 投入 | 产出 |
|---|---|---|
| **A. 启动 WP1 收敛门**(我写好脚本/配置/PASS 判据交 PRO6000 agent) | 60 GPU-h | 确认联赛能 work,门过才值得投钱 |
| B. 先实现 WP2 外部 baseline(QMIX/MAPPO/经典控制器适配本环境) | ~1–2 周 工程 | 主对比就绪 |
| C. 先做 WP3.1 CRLB 锚点(几乎零 GPU,纯分析+绘图) | 2–3 天 | 立刻提升物理可信度,低风险 |
| D. 我把本方案细化成可执行的 agent 指令包(每个 WP 配 config + 验证脚本) | — | PRO6000 agent 可直接跑 |

**推荐顺序:A(门)→ C(低成本加分)→ B(命门对比)→ WP3.2/4/5。**
