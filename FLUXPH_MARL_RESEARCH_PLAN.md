# FLUXPH 研究方案 — IQ 级高干扰下的两队多智能体相控阵协同对抗(给 PRO6000)

**不变 thesis(以此为准,不再每轮重推)**:系统 = **2 队 × 每队 2 部 IQ 级 侦/干/探/通 一体化相控阵 + 指挥官 + 激光 = MARL 问题**(队内 CTDE 协同 + 队间自博弈)。
① **经典估计/检测在 IQ 级高干扰下失效 —— 核心是敌扰(敌方定向干扰)+ 他扰(4 部雷达密集多辐射源共道干扰,含队内互扰;自扰小、非重点),仿真器原生;实践 ground truth,是前提不是待验证**;
② **两雷达对一对自适应敌雷达的去中心协同调度 = 无经典最优解的 MARL POMDP**。
**学习式多智能体指挥官(CTDE 协同 + 自博弈)提供经典给不了的价值。RL 立命点 = 高干扰下的多智能体协同/博弈,不是"打赢估计器"。**
**姿态 = demonstrate 不 gate**:配真高干扰 regime → 记录经典崩 → 学习式协同扛住。**经典若赢 = 干扰场景没配够狠,修场景不退。**
**venue**:高价值一区在桌(实践接地 + 多 AGENT + MARL 文献,不可被"没比强 baseline"驳,因立命点在协同不在估计);TAES/IET/EAAI 均对口两队×多功能×CTDE×自博弈。

---

## ★ 架构第一要务:干扰必须是仿真器原生 IQ 级密集多辐射源,不是 jam_mul 标量
**历史病根**:之前 RL 跑的 `twoteam_env` 用 `jam_mul`(标量噪声乘子)—— **把 IQ 级的密集多辐射源干扰物理抽象掉了 → 太干净 → 7 次经典赢。**
**修法(不手搓)**:env 的干扰/量测质量必须**由 IQ 级仿真器的原生物理算出**——4 部雷达(2 队×2)同时发射(侦/干/探/通),每部接收机看到:
- **敌扰(核心)**:敌方定向把 `干` 功能对准你 → 压制你的传感;
- **他扰(核心)**:所有其它辐射源(敌方的侦/探/通发射 + **你自己的队友**)落进你接收带/波束的共道干扰——**非定向,但污染你的接收机**;
- 自扰:小,非重点(SIC 或物理上就小),不纠结;
- **他扰含队内互扰是关键**:你两部雷达若同频/同向发射会**互相致盲** → 这正是协同问题的物理来源;
- track σ / 检测质量 = **这个密集多辐射源环境的后果**,耦合所有 4 部雷达的孔径/频率/波束决策;
- **速度张力解法**:IQ sim 算物理(4 源干扰叠加),**RL 在派生状态(干扰退化后的量测/观测)上决策孔径分配,不处理原始 IQ 样本** → 忠实 + 可训。

---

## WP-A — 把 IQ 原生干扰接进两队 MARL env(修抽象丢物理,~1.5-2 周)
1. env 接口对接 IQ 级仿真器的干扰计算(或忠实派生模型):4 部雷达孔径分配(侦/干/探/通)→ 各自发射 → **IQ 物理算敌扰(定向)+ 他扰(共道多源叠加,含队内)→ 退化量测**;
2. **每队 2 雷达 + 多基地融合**:2 雷达几何分集 → 融合定位;但融合被敌扰 + 队内他扰争夺(通信链 + track vs jam 孔径权衡 + **两雷达频率/波束错开避互扰**);
3. 激光驻留-kill-链 + 暴露/home-on-jam(复用);
4. **验证(必过)**:① 敌发射定向干扰→你 track 退化(敌扰生效)② **队友同频/同向发射→你他扰 σ 上升(队内互扰生效,协同的物理来源)** ③ 4 源共道叠加正确 ④ 镜像自博弈无偏(胜率≈0.5)⑤ NaN-free、adv_std∈[3,14]。

## WP-B — 配高干扰 regime 到实践现实(demonstrate 前提,不 gate)
1. 把敌扰(定向 JSR)+ 他扰(共道多源密度,含队内)强度配到**实践反映经典失效的量级**(参考真实 JSR/共道干扰密度);
2. **姿态**:目标是**复现"经典已知失效"的现实**;若经典没崩 → **干扰配得不够狠,加大到现实,不退**;
3. **验证**:此 regime 下经典估计(固定 Q KF / IMM-PDAF)**发散/track 塌/检测失效** —— 记录 practitioner-reported 的失效,作为 demonstrate 的起点。

## WP-C — 强经典多智能体基线(anti-strawman + 记录它怎么崩)
**必须强(否则赢了不可信),且按 thesis 它会崩:**
1. **robust 估计 + 经典抗干扰**:robust-KF / 自适应 Q / 频率捷变 + 自适应波束置零(对敌扰/他扰的经典最优反制也给足);
2. **中心化/规则 2 雷达协同**:water-fill/Q-RAM 分配 + 规则式两雷达任务分工(谁跟/谁干)+ 规则式频率/波束错开(避队内他扰);
3. **记录它在哪崩、为什么崩**:估计层(重非平稳敌扰+他扰下 robust 估计也发散)+ **协同层(中心化/规则协同处理不了"去中心避队内互扰 + 穿透敌扰"的对抗非平稳 MARL——这是估计之外的独立失效轴)**。

## WP-D — 学习式 MARL 指挥官(贡献,立命点在协同)
1. **架构**:队内 **CTDE**(中央 critic 协调 2 雷达,α_eff noise-robust)+ 队间 **自博弈/league**;
2. **obs**:每雷达(自己 track 均值 + trace_P + 收到的敌扰/他扰 JSR 分带 + 队友当前频率/波束(避互扰的关键) + kill 进度 + 暴露);privileged critic 加敌方/全局;
3. **action**:每雷达 25 子阵 → 四功能分配 + 波束指向 + **频率选择(避队内他扰 + 穿透敌扰)** + 激光目标 + 辐射控制;
4. **协同立命点(headline)**:学习"**2 雷达互补 + 避队内互扰**"——(a)频率/波束错开使自己两部雷达**不互相致盲(避他扰)**;(b)一部传感一部干扰保护它、被敌扰压制时另一部接管;(c)频率/空间分集穿透敌扰——**这是中心化/规则经典做不好的去中心 MARL 协同,也是本工作的核心贡献**;
5. **训练**:PPO/MAPPO(CTDE),log_std_floor=-6,league/PFSP(update_pool_winrate EMA),checkpoint→`checkpoints/twoteam_iq/`(**严禁 /tmp**),**priv[:,4] 归一化 assert**;num_envs 按 IQ 物理吞吐定。

## WP-E — cross-play 评测 + 刻画(demonstrate MARL 优势)
1. **cross-play 锦标赛**:{学习式 MARL, 强经典(robust 估计+捷变置零+中心化协同), MAPPO 无 league, IPPO} 互打,双向平均 + 共同 held-out;
2. **核心对比**:高干扰 regime 下 **学习式 MARL vs 强经典** 的 mission(kill/survival)+ track vs CRLB + **协同质量**;
3. **消融(证立命点在协同)**:−CTDE(退 IPPO,证队内协同价值)、−league(证对抗自博弈价值)、−多基地(单雷达,证 2 雷达协同价值)、**−队内他扰建模(证"避互扰"是协同价值来源)**、−频率选择(证捷变抗敌扰)、−belief(证 trace_P 条件化);
4. **操作包线**:mission 能力 vs 干扰强度——**强经典在某干扰阈崩,学习式 MARL 扩包线**(headline 图);
5. 统计:≥5 seed,bootstrap 1e4 CI,Welch-t/Mann-Whitney,Cohen's d,Holm-Bonferroni,Elo±CI。

---

## 详细实验计划(实验矩阵 + 图表清单)
**统一变量(除算法外全控)**:同一 IQ 物理、同一干扰生成、同一几何分布集、同一计算预算、同 seed 集(≥5)、同 held-out 对手集。

### 方法集(4)
| 方法 | 说明 | 消融角色 |
|---|---|---|
| **学习式 MARL** | CTDE 协同 + 自博弈/league | 主方法 |
| **强经典多智能体** | robust 估计 + 捷变/波束置零 + 中心化/规则协同 | anti-strawman(thesis:高干扰崩) |
| **MAPPO(无 league)** | CTDE 但无对抗自博弈 | 证 league/对抗价值 |
| **IPPO** | 无 CTDE | 证队内协同价值 |

### 干扰 regime 轴(主自变量,量级标定于 WP-B)
敌扰(定向 JSR)low→high 扫全程(含 low=经典够用区,诚实报)× 他扰密度(共道多辐射源,含队内)× 几何(镜像+随机)× (次轴)目标数/机动。**姿态:配到实践现实的高干扰(经典已知失效);经典没崩=加大到现实。**

### 实验序列(每个产论文图/表)
| # | 实验 | 产物 |
|---|---|---|
| **E0**(WP-A 验证) | 干扰物理生效:敌扰使 track 退化 / **队友同频→你他扰 σ 升(队内互扰)** / 4 源共道叠加 / 镜像无偏 | **Fig2** |
| **E1(headline)**(WP-B+E) | 4 方法 × 干扰全扫 → mission(kill/survival)vs 干扰强度 → **操作包线图**(经典在 D_c 崩,学习式 MARL 扩 ΔD) | **Fig3 + Table1** |
| **E2**(WP-C) | 强经典崩的**双轴分解**:估计轴(robust 估计在重非平稳干扰发散)+ **协同轴**(中心化/规则协同做不了"去中心避互扰+穿透敌扰") | **Fig4** |
| **E3(立命点)**(WP-D/E) | **协同分析**:学习式 MARL 的 2 雷达做了什么——频率/波束分集避队内互扰、互补角色(一传感一保护)、被压制时接管 → 定性轨迹 + 定量协同指标 | **Fig5** |
| **E4(消融)** | −CTDE / −league / **−队内他扰建模** / −频率选择 / −belief → 每个掉多少 gap(证价值来源在协同) | **Fig6 + Table2** |
| **E5** | cross-play 锦标赛 + Elo + exploitability(对抗鲁棒) | **Fig7** |
| **E6** | track 质量 vs 多基地 CRLB/PCRLB across 干扰(物理锚) | **Fig8** |

### 指标定义
- **mission**:kill-rate、time-to-kill、survival-rate;
- **协同(立命点)**:队内互扰事件率↓、功能分配多样性、mutual-protection 触发率;
- **估计**:track RMSE / trace_P vs CRLB;
- **对抗**:cross-play 胜率、Elo、exploitability/Nash-gap;
- **统计**:mean±95%CI(bootstrap 1e4)、Welch-t/Mann-Whitney、Cohen's d、Holm-Bonferroni、D_c bootstrap CI。

### 论文骨架(图表→章节)
1 Intro(认知雷达一体化 under 高干扰 + 多 AGENT 协同缺口)· 2 Related(Li'22/Xiong'23/Dolinger'25 + SMAC/CTDE + 认知雷达抗干扰)· 3 系统/testbed(Fig1-2,IQ 原生干扰 + MATLAB 校验 + CRLB)· 4 方法(学习式 MARL:CTDE+自博弈+避互扰频率选择)· 5 实验(Fig3-8:操作包线 + 经典双轴崩 + 协同分析 + 消融 + cross-play + CRLB)· 6 讨论(工程可部署 + 局限仿真 + 双用途)。

### 诚实标注(TBD,不造假精确)
- **干扰 dB 级别、hop 参数、hyperparams:标定于 WP-A/B,现不编数字**;
- **IQ sim 接口/吞吐:WP-A 落地时定;若 IQ 全物理训 RL 太慢,派生"忠实保留敌扰+他扰"的降维态(非标量 jam_mul)**。

---

## 决策(demonstrate,不是 retreat-gate)
| 结果 | 去向 |
|---|---|
| **学习式 MARL 在高干扰 regime 显著扛住 + 协同出经典给不了的能力(mission 赢 + 消融证协同/对抗是来源)** | **命题成立 → 高价值一区(实践接地+多AGENT+MARL文献)** |
| 强经典在高干扰**没崩** | **干扰场景没配够狠(WP-B)→ 加大到实践现实重跑,不退** |
| 配到实践现实、强经典崩、但学习式也扛不住 | 才是真难题(信息极限)→ 诚实分析为什么(可能 SIC 有物理下界)|

## 规避的历史 bug(逐条,不重蹈)
- **jam_mul 标量 → IQ 原生干扰**(WP-A 核心);
- α_eff:priv[:,4] 归一化 assert;PFSP:update_pool_winrate EMA;checkpoint 非 /tmp;log_std_floor=-6;
- cross-play 双向平均 + held-out;NaN/adv_std/entropy 逐轮监控;
- **别 gate 测"经典会不会失效"**——经典失效是前提,是 demonstrate;经典赢=修场景;
- 强经典非稻草人(robust+SIC+中心化协同)。

## GPU 预算 + 时间线
| WP | 内容 | 周 |
|---|---|---|
| WP-A | IQ 原生干扰接进 env + 验证 | 1.5-2 |
| WP-B | 配高干扰 regime + 记录经典崩 | 0.5-1 |
| WP-C | 强经典(robust+SIC+中心化协同)基线 | 1.5 |
| WP-D | 学习式 MARL 指挥官训练 | 2-3 |
| WP-E | cross-play + 消融 + 统计 | 2-3 |
| 合计 | | **~2.5-3.5 月** |

## 回报格式
每 WP 贴回:WP-A 干扰物理验证(自/他/敌扰生效 + 镜像无偏)→ WP-B 经典在高干扰崩的证据 → WP-C 强经典崩的机制(估计轴 + 协同轴)→ WP-D 学习式训练健康 → WP-E cross-play 矩阵 + Elo + 高干扰 mission 对比 + **消融(证协同/对抗/自扰是价值来源)** + 统计。

## 诚实钉死
- **立命点在多智能体协同/博弈,不在估计**——消融必须证明赢来自 CTDE 协同 + 自博弈对抗(去 CTDE/去 league 掉 gap),否则又变成"打赢估计器"的老路;
- **干扰必须 IQ 原生**——若又退回 jam_mul 标量,等于复现 7 次失败;
- **demonstrate 不 gate**——经典失效是实践前提;若我们的 sim 里经典赢,是场景配错,不是经典胜利。
