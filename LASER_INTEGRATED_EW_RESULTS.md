# 一体化 EW 竞速博弈 — 结果与 CTDE 消融

**日期:** 2026-06-17
**平台:** 本机 RTX 4090 D,num_envs=2,~130–260s/轮,30 PSRO 轮
**分支:** evo/laser-fix
**配置:** `configs/laser_25x25_ew_race.yaml`(baseline) vs `laser_25x25_ew_race_ctde.yaml`(CTDE)

把目标从"0.2m 精确击杀"重构为**用户的真实意图**:0.2m 容差不是目的,而是**逼迫多 agent 通过干扰/侦查/探测/通信,在保存自己的条件下尽快激光击杀对方任意雷达**的强制函数。本文记录 ①竞速奖励 + ②干扰动力学 的实现,以及让干扰被"学会"的 CTDE 信用通道。

---

## 1. 机制(①②)

**① 竞速奖励**(`_get_rewards`):
- `race_time_cost`(每步,博弈未决时)→ 尽快击杀;
- `race_death_penalty`(我方雷达被毁额外重罚,叠加 env 的 -10)→ 保存自己;
- `jam_cost`(每步每单位干扰)→ 干扰要付辐射代价,需权衡。

**② 干扰动力学**(`_fuse_one`/`_fused_sensing`):
- 敌方干扰 `jam∈[0,1]` 通过 `×(1+gain·jam)` **同时抬高我方距离和横向测量 σ**(噪声干扰降有效 SNR → 所有测量都糊)。
- 关键:**只抬横向无效**——好的 5km 部署基线下融合靠距离精度,只抬横向几乎不影响(单测 0.12→0.14m);抬两者后半干扰即把定位从 0.06m 抬到 0.31m,**拒止 0.2m 击杀**。
- 干扰由 commander 动作 `[4]` 经梯度学出(不强制),`jam_level=(action[4]+1)/2`。

**干扰课程**(关键稳定性修复):
- Phase A(kr>0.5m):**jam_gain=0** + jam 维随 aim 退火 → 干净复现已验证的"学会 0.2m 精确击杀 + kr 课程 50→0.2m"。
- Phase B(kr≤0.5m):jam_gain=8 + jam 维**持高探索**(不退火)→ 紧 kr 下看干扰涌现。
- 若从 iter0 就持高 jam 探索:其高方差梯度经**共享 trunk** 扰动 fire 头 → 确定性 eval 不开火 → kr 冻死在 50m。Phase 分离 + `decouple_value` 解决。

---

## 2. 核心问题:干扰有效果,却没被"学会"

**Baseline(无 CTDE)Phase B 结果:**

| iter | jam | 红胜 | 蓝胜 | 平局 |
|---|---|---|---|---|
| 14(jam 开) | 0.51 | 0.89 | 0.11 | 0.00 |
| 30 | 0.50 | **0.48** | **0.35** | 0.17 |

- `jam` **全程死守 0.50**(= 动作 0 基线,零涌现);
- 互扰造成平局(0→0.17),红队统治**单调瓦解** 0.88→0.48,蓝队升到 0.35。

**根因:对手中介的信用分配。** 干扰收益是"我干扰→敌方致盲→敌方打不到我→我存活",这条因果链经过**对手的观测与策略**,PPO 用我自身奖励的优势函数够不到;而 jam_cost 是即时负梯度 → 净梯度近零 → jam 停在基线。

---

## 3. 解法:CTDE 干扰感知中央 critic

把指挥官特权 value 头从 5 维扩到 **9 维**,加入干扰态。核心特征:

```
[7] enemy_blindness_to_me = (1 + gain·my_jam) / (1 + gain_ref)
    随【我的】干扰单调上升;我安全(敌方看不到我)时取高值。
```

中央 critic 学到"此态价值高",GAE 沿值自举把**我的存活归因到我的干扰** → 策略梯度终于够到 jam 动作。`decouple_value` 保证这个更富的 critic 用独立 value trunk,**不扰动 actor trunk**(避开第 1 节的耦合)。

**CTDE(priv_dim=9)Phase B 结果:**

| iter | jam | 红胜 | 蓝胜 | 平局 |
|---|---|---|---|---|
| 15(jam 开) | 0.51 | 0.93 | 0.00 | 0.07 |
| 30 | 0.52 | **0.68** | **0.02** | 0.30 |

---

## 4. A/B 对比(两 run 仅差 commander_privileged_dim)

| 指标(iter30) | Baseline | **CTDE** | 变化 |
|---|---|---|---|
| jam(eval 均值) | 0.50 不动 | 0.52 略升 | — |
| 红胜(训练方) | 0.48(瓦解) | **0.68**(守住) | +0.20 |
| **蓝胜(输给过去自己)** | **0.35** | **0.02** | **17× 降** |
| 平局 | 0.17 | 0.30 | +0.13 |

**头条:CTDE 把"输给对手"从 35% 压到 2%。** 训练方学会**保存自己**——把本会被击杀的局**转化为平局**(互扰拒止),而非被打死。直接实现"在保存自己的条件下击杀"。

**关键洞察:干扰被学成防守性行为(losses→draws),而非拉满进攻(jam 仅 0.50→0.52)。** jam 均值小动但胜负动态剧变,说明策略在**择时干扰**——eval 确定性均值看不出时序。与"不要恒定开火"一脉相承:学的是时机,不是幅度。

**诚实的混淆点:** CTDE 同时带来 (a) 更富的基础 critic(bht/距离/存活,baseline 普通 value 头看不到)和 (b) 干扰专属特征。隔离 (b) 需第三个 run(priv_dim=5 vs 9 消融)。

---

## 5. 下一步

1. **(可选)干净消融**:priv_dim=5(基础 CTDE,无干扰特征) vs 9,隔离干扰特征贡献;
2. **③ 辐射暴露权衡**:干扰/照射会让自己**更易被探测→更易受击** → 干扰从"近乎免费的防守"变成**真正的择时博弈**(干扰拒敌,但每次干扰暴露自己)。这是让干扰成为战略选择的关键张力;
3. **④ 去真值 + 真探测/通信**:移除 ground-truth,真频谱探测 + BPSK 通信闭环(§5 ISAC);
4. **jam 时序诊断**:记录"威胁条件下的 jam 水平",直接看择时干扰(eval 均值掩盖了时序)。
