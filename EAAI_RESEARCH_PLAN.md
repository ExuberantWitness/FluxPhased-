# EAAI 研究计划 — 时间交错经典+RL 的认知雷达资源管理:操作包线相变刻画

> **目标期刊**: Engineering Applications of Artificial Intelligence (Q1)
> **核心 thesis**: 经典雷达资源管理调度器在自适应电子战下有一个**明确的失效阈值**;我们提出**时间交错经典+RL 框架(Concerto-RRM)把可用操作包线扩展到该阈值之外**,并用**难度扫描—相变刻画**严谨证明"何时/何处"框架优于经典——在 MATLAB 校验的 IQ 级多功能相控阵基准上。

---

## 0. 为什么这个故事对 EAAI 成立(一段话)
EAAI 要"AI 对工程问题**超越/改进经典法**"。我们前期严谨证明:**问题简单时纯 RL 打不过经典**(共享优秀传感→控制近平凡)。本计划把贡献从"某点赢经典"(弱)升级为"**刻画经典失效边界 + 用 AI 扩展工作包线**"(强、reviewer-proof、诚实):低难度承认无优势,临界处经典断崖,高难度混合框架优雅扩展。这正是 EAAI 的"用 AI 扩展经典工程方法工作包线"黄金模板。

---

## 1. 问题定义
认知多功能相控阵雷达在**单孔径时分复用**下,须在**侦查/跟踪/干扰/通信**间分配资源,面对**自适应干扰对手** + **紧 QoS 约束**。
- **经典 QoS-RRM**(规则/贪心/MPC 调度器)是部署标准,但**固定**——自适应威胁下无法重调度;
- **纯 RL**:不稳/退化成单一功能/样本低效,且简单时打不过经典;
- **空白**:没有框架能(a)在自适应 EW 下**可证地扩展经典包线**,(b)保留经典的稳定/可解释,(c)严谨刻画优势 regime。

## 2. 贡献
| | 贡献 | 性质 |
|---|---|---|
| **C0** | IQ 级四功能 MFAR EW 对抗 MARL 基准(MATLAB 83/83 校验) | 环境/基准 |
| **C1** | 多基地 Kalman 融合→亚米定位 + CRLB 验证 | 传感/工程 |
| **C2** | **Concerto-RRM**:事件触发时间交错经典 QoS 调度器 + 噪声鲁棒 CTDE | 方法(headline) |
| **C3** | **操作包线相变刻画方法学**:难度扫描证明框架扩展经典失效边界 | 方法学(headline 图) |

## 3. 方法(C2)
### 3.1 Concerto-RRM 架构(迁移改进 CRL2RT arXiv:2502.10429)
调度时间轴 = 控制步。三角色分时掌控:
- **经典 QoS-RRM 调度器掌控标称时隙**:按各功能 QoS 余量+优先级分配 25 子阵(5×5)给侦/干/通/探/track——**按构造平衡四功能 → 结构上防退化**;
- **RL 掌控争用时隙**:重调度孔径(反干扰/重分配/欺骗);
- **RL buffer 只收 RL 时隙**(照 CRL2RT:RL 只训自己的步)→ 样本少、收敛快。

### 3.2 事件触发编排器(超越 CRL2RT 固定交替的创新)
```
每步 owner = RL  当且仅当  JSR>θ1  OR  trace(Kalman协方差 _trk_P)膨胀>θ2  OR  某功能QoS余量<ε
            → 接下来 K 步归 RL;否则经典。
```
指标全可观测。**贡献 = 自适应编排(干扰活跃多给 RL,标称经典主导)vs CRL2RT 的固定 t%2。**
超参:θ1/θ2/ε/K(扫描 §5.5)。

### 3.3 噪声鲁棒 CTDE(争用时隙策略;由 CTDE-反转发现驱动)
前期实测:含噪 anchor 下中心化 critic 学错信用分配、MAPPO 输 IPPO。机制修复:**用协方差 trace(P) 反向加权 team-advantage**:
```
α_eff = α_max · exp(-β · trace(P_norm))     # 不确定度高(干扰)→ 降 team-critic 权重、更信 per-agent
adv = (1-α_eff)·A_agent + α_eff·A_team
```
**同一个 trace(P) 信号同时驱动编排器(§3.2)和 critic 加权(§3.3)——优雅、机理一致。**

## 4. 难度参数化(相变的自变量,严谨性核心)
⚠️ **主难度轴 = 对手自适应智能度,不是纯噪声**(纯观测噪声共享前端→两边一起降→开不出 gap;因果 gap 来自"固定调度跟不上自适应对手")。

### 4.1 主轴:对手自适应阶梯(env 参数化)
| 档 | 对手行为 | env 参数 |
|---|---|---|
| L0 静态 | 固定压制式干扰 | jam pattern 固定 |
| L1 反应式 | 检测雷达在用哪个功能→干扰它,延迟 τ | 反应延迟 τ(步) |
| L2 快反应 | τ→0 | τ 连续降 |
| L3 预测/学习 | **自博弈/联赛共训的自适应干扰机**预判并抢先 | 联赛对手池 |
> **难度连续量 = 反应延迟 τ 与 智能档 L0→L3**。**联赛在此复活**:L3 的自适应对手种群由自博弈/PFSP 生成——联赛的价值(对多样自适应对手鲁棒)正好在高难度端显现。

### 4.2 次轴(证优势非单轴伪影)
QoS 约束紧度、JSR 幅度、目标机动性。

## 5. 相变实验设计(headline)
### 5.1 扫描协议
沿主轴(τ / 智能档)**扫全程**(如 τ ∈ {∞,16,8,4,2,1,0} 步 + L3),每档 ≥5 seed。**报告完整曲线,含低难度经典够用的区间**(诚实,防"调难度让经典输"的 p-hacking 质疑)。

### 5.2 对比方法(强 classical 硬要求)
| 方法 | 说明 |
|---|---|
| **强经典 QoS-RRM** | **调好的**规则/贪心/MPC 调度器(非稻草人;低难度须近最优) |
| 纯 MAPPO / 纯 IPPO | 标准 MARL |
| **固定交错 Concerto(v1)** | 复现 CRL2RT |
| **事件触发 Concerto(v2,本方法)** | 自适应编排 + 噪声鲁棒 CTDE |

### 5.3 操作包线指标
- **D_c^method** = 该方法 QoS 满足度跌破可接受阈值的难度;
- **包线扩展 ΔD = D_c^hybrid − D_c^classical**(核心贡献量);
- **优势区面积** = ∫_{D_c^classical}^{D_max}(QoS_hybrid − QoS_classical) dD;
- **低难度差** ≈ 0(诚实汇报)。

### 5.4 Headline 图
`QoS 满足度 vs 难度(自适应性)`,四条曲线 + 阴影 95%CI + 标出各 D_c + ΔD 注释。一图讲清"经典有失效边界,我们把它后推 ΔD"。

### 5.5 消融
v2 −事件触发(退 v1)、−噪声鲁棒 critic(退 fixed α)、composer 阈值(θ/K)扫描、经典调度器强弱对照。

### 5.6 统计
≥5 seed;mean±95%CI(bootstrap 1e4);D_c 处 Welch-t / Mann-Whitney;效应量 Cohen's d;多重比较 Holm-Bonferroni;曲线交叉点 D_c 用 bootstrap 置信区间。

## 6. QoS 目标函数定义
每功能定义能力需求(detect: Pd@given Pfa / SNR 阈;track: 协方差/RMSE 阈;comm: CRC/吞吐;jam: 对敌有效性)。**QoS 满足度 = 满足需求的功能占比**(或加权标量)。**目标 = 最大化 QoS-under-JSR,非击杀率**——从根上避免退化。

## 7. 物理严谨性(EAAI 工程真实性,近零 GPU)
- **CRLB 锚点**(`WP3_CRLB_ANCHOR.md`):定位精度对多基地 CRLB;
- **损伤鲁棒**:杂波/多径/量测延迟扫描;
- 强化"高保真、可部署"论证(对纯仿真的必要防御)。

## 8. GPU 预算 + 时间线
| 阶段 | 内容 | GPU-h | 周 |
|---|---|---|---|
| **Pilot(门)** | 经典QoS调度器 + v2最小实现,3 档难度证"经典失效+混合赢+不退化+快" | ~40 | 1-2 |
| WP-A 全扫描主对比 | 4 方法 × 7 难度档 × 5 seed(RL 只训争用时隙→单 run 快) | ~600 | 3-4 |
| WP-B 消融+次轴 | v2 消融 + QoS紧度/机动 | ~400 | 2-3 |
| WP-C 物理+写作 | CRLB + 损伤 + 论文 | ~200 | 4 |
| **合计** | | **~1200** | **~2.5-3 月** |

## 9. 风险登记 + 决策门
| 风险 | 缓解/门 |
|---|---|
| **主轴选错开不出 gap** | 主轴=自适应性非纯噪声(§4);Pilot 先证 gap 存在 |
| 经典太弱→不可信 | 强 classical(§5.2),低难度须近最优 |
| composer 阈值/交接抖动 | θ/K/交接连续性扫描(§5.5) |
| 混合仍不赢(自适应端经典也够) | **Pilot 门证伪**;不过→退 Path A(传感 C1+基准 C0,投 IEEE TAES) |
| 全仿真+军事色彩 | CRLB/损伤真实性做满(§7);venue 兜底 TAES/Neurocomputing |

**Pilot 决策门(最先做,~1-2 周)**:3 档难度(L0/L1/L3)下 `{强经典QoS调度器, 纯MAPPO, v2混合}`,看 →
① 经典在高难度 QoS 违约、② 混合高难度赢经典+赢纯MAPPO、③ 四功能驻留全程非零(不退化)、④ RL 只训争用时隙→更快。**四条全过才铺全套。**

## 10. 论文结构 + venue
1. Intro:认知雷达 RM under 自适应 EW + 经典失效边界 + AI 扩展包线;
2. Related:认知雷达 RM(Constrained DRL 2606.05526)、混合控制(CRL2RT 2502.10429、CIRL 2024)、CTDE/联赛;
3. 基准 C0(MATLAB 校验)+ 传感 C1(CRLB);
4. 方法 C2(Concerto-RRM + 事件触发 composer + 噪声鲁棒 CTDE);
5. 相变实验 C3(headline 包线图 + 消融 + 统计);
6. 讨论:可部署性、局限(仿真)、双用途;
- **venue**:EAAI/ESWA 对"认知雷达 RM RL"对口;纯传感则 IEEE TAES 兜底。

## 11. 引用锚(机理归因,非拍脑袋)
- CRL2RT(2502.10429)时间交错经典+RL;CIRL(2024)混合>纯RL/纯经典;Constrained DRL for Cognitive Radar RM(2606.05526)QoS-RRM;SP-PSRO(2024)/OOM(ICLR2024)自适应对手;MAPPO(2022)/CTDE 综述。
