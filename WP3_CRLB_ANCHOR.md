# WP3.1 — CRLB 物理锚点(思路,非代码)

**一句话**:把"亚米定位"从**仿真断言**升级为**物理可达性证明**——证明学到的定位精度逼近
**Cramér–Rao 下界(CRLB)**。这是**单位 GPU 成本最高的可信度提升**(CRLB 是解析的,几乎不烧卡),
也是 EAAI 审稿最认的"工程严谨"动作。

## 1. 为什么是 CRLB(它能license哪些claim)
CRLB 是给定量测模型与几何下,**任何无偏估计器**位置方差的理论下界(雷达/定位领域的标准严谨工具)。
做这个分析能合法地写出三条强 claim:
- "我们的亚米定位**不违反物理下界**"(不是仿真作弊);
- "**≥5km 部署基线(C1)是可证明最优的几何杠杆**"——CRLB 在近共线几何爆炸、在大基线收到亚米;
- "学到的控制器在定位上**接近 Bayes 最优**(达 CRLB 的 X 倍以内)"。

并且——**win_rate=0.5 那个 bug 本质就是 CRLB 在近共线几何下 det(FIM)→0 爆炸**。CRLB 分析把那次故障
反转成一个**有原理的贡献**:"我们识别出 GDOP 退化是失效模式,部署基线强制是物理上正确的解,并用 CRLB 验证。"

## 2. 推导思路(概念链,不写代码)
1. **量测模型**:每部雷达对目标测**距离**(σ_r = `range_sigma_m` = 0.05m,cm 级准)和**横向/角度**
   (σ_cr = `crossrange_factor`×R = 7.4e-5×R,几十米糊)。这是一个**各向异性**协方差:沿视线(LOS)准、垂直 LOS 糊。
2. **单雷达信息贡献**:把该雷达的逆量测协方差,经其 **LOS 雅可比**投影到笛卡尔平面 →
   一个沿 LOS 方向的"信息椭圆"(LOS 方向窄/信息大,横向宽/信息小)。
3. **Fisher 信息矩阵(FIM)**:对该队**所有雷达求和**。两部雷达 LOS **夹角越大(基线越长)**,
   两个各向异性椭圆**正交叠加** → FIM 良态;近共线 → 两椭圆几乎重合 → FIM 接近奇异。
4. **CRLB = inv(FIM)**:位置误差协方差下界;定位 RMSE 下界 = sqrt(trace(CRLB))。
5. **跟踪模式的正确下界**:`mode=fused`(静态融合)用标准 CRLB;`mode=tracked`(Kalman 时间平均)
   要用 **后验 CRLB / 滤波信息下界(PCRB)**——别拿静态 CRLB 套时间平均结果,否则会被审稿挑错。

## 3. 要扫的变量 + 要画的图(论文 figure)
- **Fig A(核心)**:CRLB 定位 RMSE **vs 部署基线**(0→5→10km),以目标距离/SNR 为参数曲线族;
  应清晰看到 **≥5km 的"膝点"**;**叠上实测 achieved RMSE**(跑训练好的控制器 eval 多局测真误差)。
- **Fig B(直觉)**:近共线 vs 展开几何的 **CRLB 误差椭圆**对比图(GDOP 可视化)。
- **Fig C(可选)**:achieved RMSE / CRLB **比值 vs SNR / crossrange**(S-300/400 真实参数 vs toy 参数)。

## 4. 通过判据(license"近最优"claim)
| 指标 | 阈值 |
|---|---|
| achieved RMSE / CRLB | ≤ 1.3×(逼近下界即可声称近最优) |
| 基线膝点 | CRLB 曲线在 ~5km 处明显拐到亚米,佐证 C1 |
| 共线退化 | 小基线处 CRLB 爆到 >10m,佐证 0.5-bug 的物理根因 |

## 5. 实施成本与顺序
- **几乎零 GPU**:CRLB 是小型线性代数(解析);achieved RMSE 只需若干 eval rollout(便宜)。
- **可与 WP1 并行**:WP1 在 GPU 上跑收敛门时,这个在 CPU 上算 + 画图。
- **低风险高回报**:即便 WP1/联赛出问题,这块也独立成立,直接进论文的"传感前端"与"C1"两节。

## 6. 必引文献(把 claim 钉到文献)
- Godrich, Haimovich, Blum — MIMO/多基地雷达 CRLB;
- Bishop et al. — 定位 GDOP / 最优几何;
- Nguyen et al. — 多基地最优部署几何;
- Van Trees / Tichavský — (后验)CRLB、滤波信息下界;
- (本项目已有)`LASER_MARL_LITERATURE.md` / `LASER_PHASED_ARRAY_LITERATURE.md` 里的相控阵物理。

## 7. 陷阱(别踩)
- CRLB 假设**无偏估计 + 已知噪声模型**;若融合/跟踪有偏,先报偏差再谈 CRLB。
- 跟踪模式用 **PCRB**,不是静态 CRLB(§2.5)。
- achieved RMSE 要对**真目标位置**算,且**排除已击杀/越界**的退化样本,口径写清。
- crossrange σ 随距离 R 线性增长——扫描时 RMSE/CRLB 都要按真实交战距离取,不能只取一个点。
